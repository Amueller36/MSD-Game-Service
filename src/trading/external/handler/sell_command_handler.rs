use crate::game::game_state::GameState;
use crate::player::PlayerState;
use crate::trading::external::command::Command;

pub fn handle_selling_command(player: &PlayerState, command: Command) {
    todo!()
}