use std::collections::HashMap;
use uuid::Uuid;
use crate::planet::planet::Planet;

pub struct MapGenerator {}

impl MapGenerator {
    fn create_map_of_size(size: u32) -> HashMap<Uuid, Planet> {
        todo!()
    }
}